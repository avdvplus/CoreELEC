PORTED from pannal af8a9d0568, rebased here onto inputstream.adaptive 21.5.22
(his targets 21.5.18) and extended with pool-id validation in DecryptSampleData.
Two of his inline comments were not carried over in that rebase.
His diffstat is omitted because it no longer matches this rebased patch.
The original header and description follow.

From af8a9d0568f4552596b9d5f4b6e84af85d171070 Mon Sep 17 00:00:00 2001
From: pannal <1359593+pannal@users.noreply.github.com>
Date: Fri, 15 May 2026 02:11:12 +0200
Subject: [PATCH] [ClearKey] Backport CBCS support for HLS+ClearKey playback

Ports upstream master commit 37e01a06 ([ClearKey] Implemented CBCS support)
onto the Omega branch so HLS streams using SAMPLE-AES (cbcs) with ClearKey
can be decrypted. Omega-side HLSTree already mapped SAMPLE-AES to AES_CBC
but the ClearKey decrypter:
  1) was hardcoded to refuse anything but AES_CTR, and
  2) created a single CTR-only AP4 decrypter at construction time and
     ignored the per-fragment CryptoInfo handed to SetFragmentInfo.

Rewrites CClearKeyCencSingleSampleDecrypter to a pool-based, lazy-create
flow (CTR or CBC chosen from cryptoInfo.m_mode, with resetIvEachSubsample
for CBCS). License-request flow is kept unchanged (rapidjson, same constructor
signatures, same Kodi-property semantics); storage moves to a raw-bytes
KID/KEY map. The decrypter falls back to the single available key when
EXT-X-KEY carries no KEYID (typical HLS+CK identity-keyformat case).

p3i-only; will not be sent upstream (Omega is in maintenance, the fix is
already on master 22.x).

diff --git a/inputstream.adaptive/addon.xml.in b/inputstream.adaptive/addon.xml.in
index a550d04..a47e3ca 100644
--- a/inputstream.adaptive/addon.xml.in
+++ b/inputstream.adaptive/addon.xml.in
@@ -1,7 +1,7 @@
 <?xml version="1.0" encoding="UTF-8"?>
 <addon
   id="inputstream.adaptive"
-  version="21.5.22"
+  version="21.5.99"
   name="InputStream Adaptive"
   provider-name="peak3d, Team Kodi">
   <requires>@ADDON_DEPENDS@</requires>
diff --git a/inputstream.adaptive/changelog.txt b/inputstream.adaptive/changelog.txt
index 7c3c01b..af9c4c3 100644
--- a/inputstream.adaptive/changelog.txt
+++ b/inputstream.adaptive/changelog.txt
@@ -1,3 +1,9 @@
+v21.5.99.2 (2026-08-13)
+- [ClearKey] Backport CBCS support so HLS+ClearKey (SAMPLE-AES) plays (port of upstream master 37e01a06)
+- [ClearKey] Drop AES_CTR-only gate in CreateSingleSampleDecrypter; accept AES_CBC and NONE
+- Rebased onto upstream 21.5.22 (TS PSI CRC validation, large H264 SPS/PPS extradata)
+- Custom build; not an upstream release
+
 v21.5.22 (2026-07-01)
 - Fix TS demuxer to validate PSI sections, prevent possible crashes
 - Fix TS demuxer to support large H264 SPS/PPS extradata, cause of broken playback (e.g. Wowza)
diff --git a/src/decrypters/clearkey/ClearKeyCencSingleSampleDecrypter.cpp b/src/decrypters/clearkey/ClearKeyCencSingleSampleDecrypter.cpp
index bb31e8d..755b5c9 100644
--- a/src/decrypters/clearkey/ClearKeyCencSingleSampleDecrypter.cpp
+++ b/src/decrypters/clearkey/ClearKeyCencSingleSampleDecrypter.cpp
@@ -18,6 +18,7 @@
 #include "utils/log.h"
 
 #include <algorithm>
+#include <utility>
 
 #include <rapidjson/document.h>
 #include <rapidjson/stringbuffer.h>
@@ -94,22 +95,14 @@ CClearKeyCencSingleSampleDecrypter::CClearKeyCencSingleSampleDecrypter(
     return;
   }
 
-  const std::string b64DefaultKeyId = BASE64::Encode(defaultKeyId);
-  if (!STRING::KeyExists(m_keyPairs, b64DefaultKeyId))
+  if (!STRING::KeyExists(m_kidPairs, defaultKeyId))
   {
     LOG::LogF(LOGERROR, "Key not found on license server response");
+    m_kidPairs.clear();
     return;
   }
 
-  const std::vector<uint8_t> keyBytes = BASE64::Decode(m_keyPairs[b64DefaultKeyId]);
-  if (AP4_FAILED(AP4_CencSingleSampleDecrypter::Create(AP4_CENC_CIPHER_AES_128_CTR, keyBytes.data(),
-                                                       static_cast<AP4_Size>(keyBytes.size()), 0, 0,
-                                                       nullptr, false, m_singleSampleDecrypter)))
-  {
-    LOG::LogF(LOGERROR, "Failed to create AP4_CencSingleSampleDecrypter");
-  }
   SetParentIsOwner(false);
-  AddSessionKey(defaultKeyId);
 }
 
 CClearKeyCencSingleSampleDecrypter::CClearKeyCencSingleSampleDecrypter(
@@ -132,51 +125,134 @@ CClearKeyCencSingleSampleDecrypter::CClearKeyCencSingleSampleDecrypter(
     if (STRING::KeyExists(keys, hexDefKid))
       STRING::ToHexBytes(keys.at(hexDefKid), hexKey);
     else
-      LOG::LogF(LOGERROR, "Missing KeyId \"%s\" on DRM configuration", defaultKeyId.data());
+      LOG::LogF(LOGERROR, "Missing KeyId \"%s\" on DRM configuration", hexDefKid.c_str());
   }
 
-  AP4_CencSingleSampleDecrypter::Create(AP4_CENC_CIPHER_AES_128_CTR, hexKey.data(),
-                                        static_cast<AP4_Size>(hexKey.size()), 0, 0, nullptr, false,
-                                        m_singleSampleDecrypter);
+  if (!hexKey.empty())
+    m_kidPairs.emplace(defaultKeyId, std::move(hexKey));
+
   SetParentIsOwner(false);
-  AddSessionKey(defaultKeyId);
 }
 
-void CClearKeyCencSingleSampleDecrypter::AddSessionKey(const std::vector<uint8_t>& keyId)
+bool CClearKeyCencSingleSampleDecrypter::HasKeyId(const std::vector<uint8_t>& keyid)
+{
+  return !keyid.empty() && STRING::KeyExists(m_kidPairs, keyid);
+}
+
+AP4_UI32 CClearKeyCencSingleSampleDecrypter::AddPool()
 {
-  if (std::find(m_keyIds.begin(), m_keyIds.end(), keyId) == m_keyIds.end())
-    m_keyIds.emplace_back(keyId);
+  const AP4_UI32 poolId = m_nextPoolId++;
+  m_pool.emplace(poolId, PINFO());
+  return poolId;
 }
 
-bool CClearKeyCencSingleSampleDecrypter::HasKeyId(const std::vector<uint8_t>& keyid)
+void CClearKeyCencSingleSampleDecrypter::RemovePool(AP4_UI32 poolId)
 {
-  if (!keyid.empty())
+  m_pool.erase(poolId);
+}
+
+AP4_Result CClearKeyCencSingleSampleDecrypter::SetFragmentInfo(AP4_UI32 poolId,
+                                                               const std::vector<uint8_t>& keyId,
+                                                               const AP4_UI08 nalLengthSize,
+                                                               AP4_DataBuffer& annexbSpsPps,
+                                                               AP4_UI32 flags,
+                                                               CryptoInfo cryptoInfo)
+{
+  if (!STRING::KeyExists(m_pool, poolId))
   {
-    for (const std::vector<uint8_t>& key : m_keyIds)
-    {
-      if (key == keyid)
-        return true;
-    }
+    LOG::LogF(LOGERROR, "Cannot set fragment info, pool id %u does not exist", poolId);
+    return AP4_ERROR_INVALID_PARAMETERS;
+  }
+
+  if (cryptoInfo.m_mode != CryptoMode::NONE && cryptoInfo.m_mode != CryptoMode::AES_CTR &&
+      cryptoInfo.m_mode != CryptoMode::AES_CBC)
+  {
+    LOG::LogF(LOGERROR, "Cannot set fragment info, unsupported crypto mode (%i)",
+              static_cast<int>(cryptoInfo.m_mode));
+    return AP4_ERROR_INVALID_PARAMETERS;
   }
-  return false;
+
+  PINFO& pInfo = m_pool[poolId];
+  FINFO& fInfo = pInfo.fInfo;
+
+  pInfo.isChanged = fInfo.kid != keyId || fInfo.cryptoInfo.m_mode != cryptoInfo.m_mode ||
+                    fInfo.cryptoInfo.m_cryptBlocks != cryptoInfo.m_cryptBlocks ||
+                    fInfo.cryptoInfo.m_skipBlocks != cryptoInfo.m_skipBlocks;
+
+  fInfo.kid = keyId;
+  fInfo.cryptoInfo = cryptoInfo;
+
+  return AP4_SUCCESS;
 }
 
 AP4_Result CClearKeyCencSingleSampleDecrypter::DecryptSampleData(
-    AP4_UI32 pool_id,
-    AP4_DataBuffer& data_in,
-    AP4_DataBuffer& data_out,
+    AP4_UI32 poolId,
+    AP4_DataBuffer& dataIn,
+    AP4_DataBuffer& dataOut,
     const AP4_UI08* iv,
-    unsigned int subsample_count,
-    const AP4_UI16* bytes_of_cleartext_data,
-    const AP4_UI32* bytes_of_encrypted_data)
+    unsigned int subsampleCount,
+    const AP4_UI16* bytesOfCleartextData,
+    const AP4_UI32* bytesOfEncryptedData)
 {
-  if (!m_singleSampleDecrypter)
+  if (m_pool.empty())
+  {
+    LOG::LogF(LOGERROR, "Cannot decrypt data, the pool is empty");
+    return AP4_ERROR_INTERNAL;
+  }
+  if (!STRING::KeyExists(m_pool, poolId))
+  {
+    LOG::LogF(LOGERROR, "Cannot decrypt data, pool id %u does not exist", poolId);
+    return AP4_ERROR_INVALID_PARAMETERS;
+  }
+
+  PINFO& pInfo = m_pool[poolId];
+  auto& decrypter = pInfo.decrypter;
+
+  if (!decrypter || pInfo.isChanged)
   {
-    return AP4_FAILURE;
+    const auto& fInfo = pInfo.fInfo;
+
+    std::vector<uint8_t> lookupKid = fInfo.kid;
+    if (lookupKid.empty() && m_kidPairs.size() == 1)
+      lookupKid = m_kidPairs.begin()->first;
+
+    if (!STRING::KeyExists(m_kidPairs, lookupKid))
+    {
+      LOG::LogF(LOGERROR, "Cannot decrypt data, missing KID/KEY from the license data");
+      return AP4_ERROR_INVALID_STATE;
+    }
+
+    const std::vector<uint8_t>& key = m_kidPairs[lookupKid];
+
+    AP4_UI32 cypherType{AP4_CENC_CIPHER_NONE};
+    bool resetIvEachSubsample{false};
+
+    if (fInfo.cryptoInfo.m_mode == CryptoMode::AES_CBC)
+    {
+      cypherType = AP4_CENC_CIPHER_AES_128_CBC;
+      resetIvEachSubsample = true;
+    }
+    else
+    {
+      cypherType = AP4_CENC_CIPHER_AES_128_CTR;
+    }
+
+    AP4_CencSingleSampleDecrypter* pDecrypter{nullptr};
+    if (AP4_FAILED(AP4_CencSingleSampleDecrypter::Create(
+            cypherType, key.data(), static_cast<AP4_Size>(key.size()),
+            fInfo.cryptoInfo.m_cryptBlocks, fInfo.cryptoInfo.m_skipBlocks, nullptr,
+            resetIvEachSubsample, pDecrypter)))
+    {
+      LOG::LogF(LOGERROR, "Failed to create AP4_CencSingleSampleDecrypter");
+      return AP4_ERROR_INTERNAL;
+    }
+
+    decrypter.reset(pDecrypter);
+    pInfo.isChanged = false;
   }
-  return (m_singleSampleDecrypter)
-      ->DecryptSampleData(data_in, data_out, iv, subsample_count, bytes_of_cleartext_data,
-                          bytes_of_encrypted_data);
+
+  return decrypter->DecryptSampleData(dataIn, dataOut, iv, subsampleCount, bytesOfCleartextData,
+                                      bytesOfEncryptedData);
 }
 
 std::string CClearKeyCencSingleSampleDecrypter::CreateLicenseRequest(
@@ -234,52 +310,59 @@ bool CClearKeyCencSingleSampleDecrypter::ParseLicenseResponse(std::string data)
     return false;
   }
 
-  for (auto& jChildObj : jDoc.GetObject())
+  if (jDoc.HasMember("Message") && jDoc["Message"].IsString())
   {
-    std::string b64Key;
-    std::string b64KeyId;
-    const std::string keyName = jChildObj.name.GetString();
-    rapidjson::Value& jDictVal = jChildObj.value;
+    LOG::LogF(LOGERROR, "Error in license response: %s", jDoc["Message"].GetString());
+    return false;
+  }
 
-    if (keyName == "Message" && jDictVal.IsString())
-    {
-      LOG::LogF(LOGERROR, "Error in license response: %s", jDictVal.GetString());
-      return false;
-    }
+  if (!jDoc.HasMember("keys") || !jDoc["keys"].IsArray())
+  {
+    LOG::LogF(LOGERROR, "No keys in license response");
+    return false;
+  }
 
-    if (!jDoc.HasMember("keys"))
+  for (auto const& jArrayKey : jDoc["keys"].GetArray())
+  {
+    if (!jArrayKey.IsObject())
+      continue;
+
+    if (jArrayKey.HasMember("kty") && jArrayKey["kty"].IsString() &&
+        std::string{jArrayKey["kty"].GetString()} != "oct")
     {
-      LOG::LogF(LOGERROR, "No keys in license response");
-      return false;
+      LOG::LogF(LOGWARNING, "Ignored JWK set, due to unsupported \"%s\" key type",
+                jArrayKey["kty"].GetString());
+      continue;
     }
 
-    if (keyName == "keys" && jDictVal.IsArray())
+    std::string b64Key;
+    std::string b64KeyId;
+
+    if (jArrayKey.HasMember("k") && jArrayKey["k"].IsString())
+      b64Key = jArrayKey["k"].GetString();
+
+    if (jArrayKey.HasMember("kid") && jArrayKey["kid"].IsString())
+      b64KeyId = jArrayKey["kid"].GetString();
+
+    if (b64Key.empty() || b64KeyId.empty())
+      continue;
+
+    CkB64Decode(b64Key);
+    BASE64::AddPadding(b64Key);
+    CkB64Decode(b64KeyId);
+    BASE64::AddPadding(b64KeyId);
+
+    const std::vector<uint8_t> kidBytes = BASE64::Decode(b64KeyId);
+    const std::vector<uint8_t> keyBytes = BASE64::Decode(b64Key);
+
+    if (kidBytes.empty() || keyBytes.empty())
     {
-      // NOTE: for now we assume that one key only is requested and one only will come in the license response
-      for (auto const& jArrayKey : jDictVal.GetArray())
-      {
-        if (jArrayKey.IsObject())
-        {
-          if (jArrayKey.HasMember("k") && jArrayKey["k"].IsString())
-            b64Key = jArrayKey["k"].GetString();
-
-          if (jArrayKey.HasMember("kid") && jArrayKey["kid"].IsString())
-            b64KeyId = jArrayKey["kid"].GetString();
-        }
-
-        if (!b64Key.empty() && !b64KeyId.empty())
-        {
-          CkB64Decode(b64Key);
-          BASE64::AddPadding(b64Key);
-
-          CkB64Decode(b64KeyId);
-          BASE64::AddPadding(b64KeyId);
-
-          m_keyPairs.emplace(b64KeyId, b64Key);
-          break;
-        }
-      }
+      LOG::LogF(LOGERROR, "Malformed key pair value in license response");
+      continue;
     }
+
+    m_kidPairs.insert_or_assign(kidBytes, keyBytes);
   }
-  return true;
+
+  return !m_kidPairs.empty();
 }
diff --git a/src/decrypters/clearkey/ClearKeyCencSingleSampleDecrypter.h b/src/decrypters/clearkey/ClearKeyCencSingleSampleDecrypter.h
index f707859..7c3388a 100644
--- a/src/decrypters/clearkey/ClearKeyCencSingleSampleDecrypter.h
+++ b/src/decrypters/clearkey/ClearKeyCencSingleSampleDecrypter.h
@@ -8,8 +8,12 @@
 
 #include "common/AdaptiveCencSampleDecrypter.h"
 #include "decrypters/IDecrypter.h"
+#include "utils/CryptoUtils.h"
 
+#include <cstdint>
 #include <map>
+#include <memory>
+#include <vector>
 
 class CClearKeyDecrypter;
 
@@ -25,35 +29,48 @@ public:
                                      const std::map<std::string, std::string>& keys,
                                      CClearKeyDecrypter* host);
   virtual ~CClearKeyCencSingleSampleDecrypter(){};
-  void AddSessionKey(const std::vector<uint8_t>& keyId);
+
   bool HasKeyId(const std::vector<uint8_t>& keyid);
-  virtual AP4_Result SetFragmentInfo(AP4_UI32 pool_id,
-                                     const std::vector<uint8_t>& key,
-                                     const AP4_UI08 nal_length_size,
-                                     AP4_DataBuffer& annexb_sps_pps,
+  virtual AP4_UI32 AddPool() override;
+  virtual void RemovePool(AP4_UI32 poolId) override;
+  virtual AP4_Result SetFragmentInfo(AP4_UI32 poolId,
+                                     const std::vector<uint8_t>& keyId,
+                                     const AP4_UI08 nalLengthSize,
+                                     AP4_DataBuffer& annexbSpsPps,
                                      AP4_UI32 flags,
-                                     CryptoInfo cryptoInfo) override
-  {
-    return AP4_SUCCESS;
-  }
-  virtual AP4_Result DecryptSampleData(AP4_UI32 pool_id,
-                                       AP4_DataBuffer& data_in,
-                                       AP4_DataBuffer& data_out,
+                                     CryptoInfo cryptoInfo) override;
+  virtual AP4_Result DecryptSampleData(AP4_UI32 poolId,
+                                       AP4_DataBuffer& dataIn,
+                                       AP4_DataBuffer& dataOut,
                                        const AP4_UI08* iv,
-                                       unsigned int subsample_count,
-                                       const AP4_UI16* bytes_of_cleartext_data,
-                                       const AP4_UI32* bytes_of_encrypted_data) override;
-  std::string CreateLicenseRequest(const std::vector<uint8_t>& defaultKeyId);
-  bool ParseLicenseResponse(std::string data);
+                                       unsigned int subsampleCount,
+                                       const AP4_UI16* bytesOfCleartextData,
+                                       const AP4_UI32* bytesOfEncryptedData) override;
   void SetDefaultKeyId(const std::vector<uint8_t>& keyId) override{};
   void AddKeyId(const std::vector<uint8_t>& keyId) override{};
-  bool HasKeys() { return !m_keyIds.empty(); }
+  bool HasKeys() { return !m_kidPairs.empty(); }
 
 private:
-  AP4_CencSingleSampleDecrypter* m_singleSampleDecrypter{nullptr};
-  std::string m_strSession;
-  std::string m_licenceDefaultKeyId;
-  std::vector<std::vector<uint8_t>> m_keyIds;
-  std::map<std::string, std::string> m_keyPairs;
+  std::string CreateLicenseRequest(const std::vector<uint8_t>& defaultKeyId);
+  bool ParseLicenseResponse(std::string data);
+
   CClearKeyDecrypter* m_host;
+
+  std::map<std::vector<uint8_t>, std::vector<uint8_t>> m_kidPairs;
+
+  struct FINFO
+  {
+    std::vector<uint8_t> kid;
+    CryptoInfo cryptoInfo;
+  };
+
+  struct PINFO
+  {
+    FINFO fInfo;
+    bool isChanged{false};
+    std::unique_ptr<AP4_CencSingleSampleDecrypter> decrypter;
+  };
+
+  std::map<AP4_UI32, PINFO> m_pool;
+  AP4_UI32 m_nextPoolId{0};
 };
diff --git a/src/decrypters/clearkey/ClearKeyDecrypter.cpp b/src/decrypters/clearkey/ClearKeyDecrypter.cpp
index cceaf61..e2f6bf6 100644
--- a/src/decrypters/clearkey/ClearKeyDecrypter.cpp
+++ b/src/decrypters/clearkey/ClearKeyDecrypter.cpp
@@ -40,9 +40,11 @@ Adaptive_CencSingleSampleDecrypter* CClearKeyDecrypter::CreateSingleSampleDecryp
     bool skipSessionMessage,
     CryptoMode cryptoMode)
 {
-  if (cryptoMode != CryptoMode::AES_CTR)
+  if (cryptoMode != CryptoMode::AES_CTR && cryptoMode != CryptoMode::AES_CBC &&
+      cryptoMode != CryptoMode::NONE)
   {
-    LOG::LogF(LOGERROR, "Cannot initialize ClearKey DRM. Only \"cenc\" encryption supported.");
+    LOG::LogF(LOGERROR, "Cannot initialize ClearKey DRM, unsupported crypto mode (%i)",
+              static_cast<int>(cryptoMode));
     return nullptr;
   }
 
