--- a/src/libbluray/bdj/java/org/videolan/Libbluray.java
+++ b/src/libbluray/bdj/java/org/videolan/Libbluray.java
@@ -55,6 +55,9 @@ public class Libbluray {

     /* hook system properties: make "user.dir" point to current Xlet home directory */

+    private static final boolean BDJ_VK_DEBUG =
+        Boolean.parseBoolean(System.getProperty("org.videolan.bdj.vkdebug", "false"));
+
     private static boolean booted;

     private static void hookProperties() {
@@ -745,6 +748,7 @@ public class Libbluray {
             break;

         case BDJ_EVENT_VK_KEY:
+            if (BDJ_VK_DEBUG) System.err.println("BDJ_EVENT_VK_KEY received: param=0x" + Integer.toHexString(param));
             switch (param & 0xffff) {
             case  0: key = KeyEvent.VK_0; break;
             case  1: key = KeyEvent.VK_1; break;
@@ -795,6 +799,7 @@ public class Libbluray {
                 if ((param & 0x20000000) != 0) {
                     r3 = EventManager.getInstance().receiveKeyEventN(KeyEvent.KEY_RELEASED, 0, key);
                 }
+                if (BDJ_VK_DEBUG) System.err.println("BDJ_EVENT_VK_KEY dispatched: key=" + key + " pressed=" + r1 + " typed=" + r2 + " released=" + r3);
                 result = r1 || r2 || r3;
             }
             break;
